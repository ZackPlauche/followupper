[build]
  command = "yarn build"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "20"

# Redirect all routes to index.html for client-side routing (SPA fallback)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

